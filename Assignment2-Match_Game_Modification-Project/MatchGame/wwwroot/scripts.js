<script>
    function updateGridLayout(gridTemplateColumns) {
        const gridContainer = document.querySelector('.grid-container');
    gridContainer.style.gridTemplateColumns = gridTemplateColumns;
    // Define a function named updateGridLayout
    // Get the HTML element with the class name '.grid-container' using document.querySelector
    // Set the gridTemplateColumns style property of the gridContainer element
    // This property controls the size and number of columns in the grid layout
    }
</script>